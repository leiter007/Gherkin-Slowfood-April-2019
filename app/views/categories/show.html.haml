#categories
  %ul
    - @categories.each do |category|
      %li= link_to category.name, category

%h1= @category.name

- @products.each do |product|
  %h2= product.name
  %p= product.description
  %p #{product.price} kr
  %p= add_to_cart(product, 1, "+")

